apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gestionar
  namespace: gestionar
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: bogdan-boicu.mibo.monster
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gestionar
                port:
                  number: 80
