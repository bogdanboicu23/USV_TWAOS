services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - db-data:/app/data
    environment:
      - DATABASE_PATH=/app/data/sarcini.db

volumes:
  db-data:
